[[Hyperland]]
├── tiling
├── scroll
├── master
└── normal window (floating with boarders)

[[rofi (rofi-wayland)]]
├── app launcher
├── clipboard (via cliphist)
├── calc
├── theme changer
├── wallpaper picker
├── wifi picker
├── bluetooth picker
├── power profiles
└── poweroff, sleep, and so on

[[waybar]]
├── top app bar
├── wifi indicator
├── tray icons
├── time and date
├── system stats (performance)
├── workspace indicator
├── bluetooth indicator
├── battery / power status
├── volume indicator
└── notification icon/count

papirus + papirus-folders (i will later make a custom isons)
├── icon theme
└── folder color variants

[[Terminal]]
├── kitty
└── wave

In Terminal
├──Fastfetch (change the ascii art)
├──btop
└──neovim (instead of kate)

Color Themes
├── Terminal
├──Opencode
└── VsCode


└── System Stats


└── Screeshots

screenshot (full screen / region select)
screenrecoder
color picker (grab hex from screen)
on-screen volume/brightness indicator
login screen / session picker
power menu UI (shutdown, reboot, logout, lock, suspend)
 wallpaper daemon (display + transitions behind rofi's picker)
generates color palette from wallpaper
syncs palette to waybar/rofi/terminal
fonts
